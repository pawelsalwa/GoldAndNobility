using Common;
using UnityEngine;

namespace ToBePlaced
{
    [PersistentComponent]
    public class asd : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
